version: '3.9'

services:
  postgres:
    image: postgres:15-alpine
    container_name: os4u_dev_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USERNAME:-os4u}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Defina DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE:-assistencia_db}
    volumes:
      - os4u_dev_postgres_data:/var/lib/postgresql/data
    ports:
      - '127.0.0.1:5433:5432'
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U os4u']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - os4u_dev_net

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: os4u_dev_backend
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - '127.0.0.1:3006:3005'
    environment:
      NODE_ENV: production
      PORT: 3005
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: ${DB_USERNAME:-os4u}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_DATABASE: ${DB_DATABASE:-assistencia_db}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: 7d
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost}
      EVOLUTION_API_URL: ${EVOLUTION_API_URL}
      EVOLUTION_API_KEY: ${EVOLUTION_API_KEY}
    volumes:
      - os4u_dev_backend_logs:/app/logs
    networks:
      - os4u_dev_net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # VITE_API_URL vazio = rota local pelo nginx na raiz
        VITE_API_URL: /api
    container_name: os4u_dev_frontend
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - os4u_dev_net

  nginx:
    image: nginx:1.25-alpine
    container_name: os4u_dev_nginx
    restart: unless-stopped
    depends_on:
      - frontend
      - backend
    ports:
      - '81:80'
    volumes:
      - ./nginx.dev.conf:/etc/nginx/nginx.conf:ro
    networks:
      - os4u_dev_net

volumes:
  os4u_dev_postgres_data:
  os4u_dev_backend_logs:

networks:
  os4u_dev_net:
    driver: bridge
